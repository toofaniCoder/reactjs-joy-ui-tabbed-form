import { Card, Stack, Typography } from "@mui/joy";

const Registration = () => {
  return (
    <Stack sx={{py:10}}>
      <Card sx={{width:0.4, mx:"auto"}}>
        <Typography level="body-md" textAlign={"center"}>Registration Page</Typography>
      </Card>
    </Stack>
  );
};

export default Registration;
